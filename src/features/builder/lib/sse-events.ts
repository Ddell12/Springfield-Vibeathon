// Re-exports from core — kept for backward compatibility with existing imports.
// New code should import from "@/core/sse-events" directly.
export { parseSSEEvent, type SSEEvent } from "@/core/sse-events";
