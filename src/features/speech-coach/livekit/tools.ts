// Speech coach tool definitions for LiveKit agent.
// Tools enable structured interactions during sessions — e.g., word picking,
// minimal pair generation, and session summary.
// Full tool implementations added in future iteration.

export const speechCoachTools: Record<string, unknown> = {};
